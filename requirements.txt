# NetGuard IDS v3.0 — dependências principais (versões pinadas)
# Atualize com cautela; rode os testes após qualquer bump de versão.

# Core web
flask==3.0.3
flask-cors>=6.0.0
flask-talisman==1.1.0

# Sistema
psutil==5.9.8

# Captura de pacotes (opcional em produção Docker)
scapy==2.5.0

# Config / utils
pyyaml==6.0.1

# Interface desktop (opcional, não necessário para SaaS headless)
pystray==0.19.5
pillow==10.3.0
plyer==2.1.0
pywebview==4.4.0
pyinstaller==6.6.0

# Billing (SaaS)
stripe==9.9.0

# Relatórios PDF
reportlab==4.2.2

# Banco de dados (PostgreSQL)
psycopg2-binary==2.9.9
