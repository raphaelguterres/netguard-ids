# NetGuard IDS — dependências para Docker/CI (Linux, sem display)
# Versões pinadas — alinhe com requirements.txt ao atualizar

# Core web
flask==3.1.3
flask-cors==6.0.2
flask-talisman==1.1.0
flask-limiter==3.8.0

# Sistema
psutil==5.9.8

# Captura de pacotes
scapy==2.7.0

# Config / utils
pyyaml==6.0.1
redis==5.0.8

# Billing (SaaS)
stripe==9.9.0

# Relatórios PDF
reportlab==4.2.2

# Banco de dados (PostgreSQL)
psycopg2-binary==2.9.9

# Testing
pytest>=7.4
pytest-cov>=4.1
